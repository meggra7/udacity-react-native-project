/**
 * Screen names as used in navigation. Note that for typing purposes these values must correspond exactly to
 * the key names in the `RootStackParamList`. If these names are ever updated, the param list should be 
 * updated as well to match.
 * 
 * @see {@link RootStackParamList}
 */
export enum Screen {
  Welcome = "Welcome",
  RegionList = "RegionList",
  CustomerList = "CustomerList",
  ViewCustomer = "ViewCustomer",
  EditCustomer = "EditCustomer",
  AddCustomer = "AddCustomer",
}
